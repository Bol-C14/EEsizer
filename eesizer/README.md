# Legacy package (deprecated)

This `eesizer` namespace is kept only for legacy notebook artifacts. New code lives in `eesizer_core`, which contains the reusable agents, toolchain, prompts, and pipeline CLI. Avoid adding new modules here; migrate to `eesizer_core` instead.
